To start the server run `yarn install` and then `yarn run start` in a terminal window.
I tested the app in the latest versions of both chrome and firefox. 

The bonus command implemented was Ellipse:
e <X> <Y> <RX> <RY>
